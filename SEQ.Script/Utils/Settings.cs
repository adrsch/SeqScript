using SEQ.Script.Core;
using Stride.Core;
using Stride.Engine.Design;
using Stride.Physics;

namespace SEQ.Script
{/*
  * TODO
    public static partial class Settings
    {
        public static SeqSettings Seq
        {
            get
            {
                var settings = Sequencer.S.Services.GetSafeServiceAs<IGameSettingsService>()?.Settings;
                return settings?.Configurations?.Get<SeqSettings>() ?? new SeqSettings();
            }
        }

        public static PhysicsSettings Physics
        {
            get
            {
                var settings = Sequencer.S.Services.GetSafeServiceAs<IGameSettingsService>()?.Settings;
                return settings?.Configurations?.Get<PhysicsSettings>() ?? new PhysicsSettings();
            }
        }
    }*/
}
